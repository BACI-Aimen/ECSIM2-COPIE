import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
  arrowText: {
    fontSize: 24,
    color: '#007AFF',
  },
});

export default styles;
